# Get input for number.
num = int(input("Please enter the number: "))

# Now I define binary , octal, hexadecimal
binary = bin(num)
octal = oct(num)
hexadecimal = hex(num)

# Output the result.
print(f"Binary:  {binary}")
print(f"octal:  {octal}")
print(f"hexadecimal:  {hexadecimal}")